version: 2.1

jobs:
  build:
    docker:
      - image: cimg/node:12.22.12-browsers #12.13.1
    steps:
      - checkout
      - run: |
          npm install --verbose
      - run: |
          npm start
